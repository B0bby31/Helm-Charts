apiVersion: v1
kind: ServiceAccount
metadata:
  name: njalla-webhook-test-sa
  namespace: {{ .Values.namespace }}
  annotations:
    helm.sh/hook-weight: "0"
    helm.sh/hook: test
    helm.sh/hook-delete-policy: before-hook-creation,hook-succeeded,hook-failed